Sandbox_EN = {
    Sandbox_ZombieFactions = "Zombie Factions",
    Sandbox_ZombieFactions_ZombieMobSize = "Zombie Mob Size",
    Sandbox_ZombieFactions_ZombieMobSize_tooltip = "Ignored unless Direct Acquisition is turned off. Maximum stable membership of a local faction mob, including its leader. The leader alone scans for enemies; followers remain dormant until the mob acquires a target or is attacked. New zombies join once within the vanilla Rally Travel Distance. 1 keeps individual acquisition. 0 removes the local membership cap. Runtime membership ends only on death, invalid identity, or faction change.",
    Sandbox_ZombieFactions_ClientCollisionDistance = "Client Collision Distance",
    Sandbox_ZombieFactions_ClientCollisionDistance_tooltip = "Diagnostic maximum planar distance in tiles at which the owning client may arm and send a collision-driven faction hit request. A real character-collision event is still required. Default 0.80; allowed range 0.25 to 2.00. The effective value is recorded in the logs and snapshotted per target grant.",
    Sandbox_ZombieFactions_ServerValidationDistance = "Server Validation Distance",
    Sandbox_ZombieFactions_ServerValidationDistance_tooltip = "Maximum server-measured planar distance in tiles at which a faction hit request may pass distance validation. Requests beyond it are rejected, and all other authority checks remain active. Default 1.60; allowed range 0.25 to 2.00. Raising this value expands the accepted damage envelope, so use controlled tests.",
    Sandbox_ZombieFactions_RetaliationRadius = "Neutral Retaliation Radius",
    Sandbox_ZombieFactions_RetaliationRadius_tooltip = "Radius in tiles around a Neutral zombie that has just taken server-validated damage, within which same-faction zombies are recruited to retaliate against that one attacker. Recruits must not already have a target, so an ongoing fight is never interrupted; the victim itself is recruited only if it is free. Membership is additionally capped by Zombie Mob Size unless that is 0. Default 8; 0 disables Neutral retaliation entirely.",
    Sandbox_ZombieFactions_RetaliationSeconds = "Neutral Retaliation Duration",
    Sandbox_ZombieFactions_RetaliationSeconds_tooltip = "Seconds a Neutral retaliation stays authorized. Any further validated hit on a member of the retaliating group restarts the timer. When it lapses the retaliation grants are released and normal Neutral behavior resumes, so retaliation never becomes permanent hostility. Hostility applies only to the individual attacker, never to its faction or its mob. Default 60; 0 disables Neutral retaliation entirely.",
    Sandbox_ZombieFactions_VerboseDiagnosticsServer = "Verbose Server Diagnostics",
    Sandbox_ZombieFactions_VerboseDiagnosticsServer_tooltip = "Log every server-side acquisition, grant, mob and damage decision instead of only the periodic summaries. Intended for diagnosing a specific defect, not for normal running. The server log has no size cap, so this is the safer of the two to enable. Default off.",
    Sandbox_ZombieFactions_VerboseDiagnosticsClient = "Verbose Client Diagnostics",
    Sandbox_ZombieFactions_VerboseDiagnosticsClient_tooltip = "Log every owner-side pursuit and impact decision instead of only the periodic summaries. Output is unbounded and has been measured at 3-4MB per minute during mass combat, which drives the client DebugLog past the size at which Project Zomboid empties it in place, losing everything written earlier in the session. Leave off for any crowd run. Default off.",
    Sandbox_ZombieFactions_DirectAcquisition = "Direct Acquisition",
    Sandbox_ZombieFactions_DirectAcquisition_tooltip = "Default. Each zombie acquires its own target through its own probe, with no mob or leader layer. Each zombie acquires its own target through its own probe, with no membership, no leader election, no shared-target arbitration and no maintenance sweep. Zombie Mob Size is ignored while this is on. Turning it off restores the older mob-and-leader targeting, which a v0.0.51 comparison measured as costing more per unit of combat, hitching roughly four times worse on its worst pass, and leaving hundreds of zombies idle. Retained for comparison only; see ADR-002.",
}
